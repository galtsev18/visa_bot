/**
 * Composition root: wire adapters and build context for application entry points.
 */

export {
  createMonitorContext,
  type MonitorContext,
  type CreateMonitorContextOptions,
} from './createMonitorContext.js';
